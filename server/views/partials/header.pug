.popup
    .popup__content
        .popup__tabs
            each i in catalogue_tabs
                a(href=i.url).popup__tab #{i.name}
        .popup__container 
            p Пока ничего нету
header.header
    .header__content
        a(href="/").header__logo
            img(src="/images/IconText.svg").logo
        .header__tabs
            each i in links
                if i.url == "/catalogue"
                    a(href=i.url).tab.header__tab #{i.name}
                else 
                    a(href=i.url).tab.header__tab #{i.name}
            .icons
                a(href="/profile").tab.header__tab
                    svg.icon(xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor")
                        path(stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z")
                a(href="/basket").tab.header__tab
                    svg.icon(xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor")
                        path(stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z")